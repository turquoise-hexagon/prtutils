#!/usr/bin/env bash
#
# create package from installed files

version=$(prt-get current "$1") 2> /dev/null || {
    echo "usage : ${0##*/} [package]" >&2
    exit 1
}

IFS=$'\n'

tar czvf "$1#$version.pkg.tar.gz" -C / -- $(
    while read -r file; do
        [[ $file =~ /$ ]] || echo "$file"
    done < <(pkginfo -l "$1")
)
